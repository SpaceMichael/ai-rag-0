# ============================================================
# ai-rag-0 — RAG 知識庫系統 Requirements
# Python 3.11+
# ============================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9        # 文件上傳支援

# --- AI Providers（多供應商，按需啟用）---
ollama==0.3.3                  # 本地 Ollama
openai==1.51.0                 # OpenAI / Azure OpenAI
google-generativeai==0.8.3     # Google Gemini
anthropic==0.34.2              # Anthropic Claude
langchain==0.3.7
langchain-community==0.3.5
langchain-ollama==0.2.1
langchain-openai==0.2.5
langchain-google-genai==2.0.4

# --- 向量數據庫（PostgreSQL + pgvector）---
pgvector==0.3.5
psycopg2-binary==2.9.9
sqlalchemy==2.0.35
alembic==1.13.3                # 數據庫 migration

# --- 文件處理（混合類型）---
pypdf2==3.0.1                  # PDF
python-docx==1.1.2             # Word (.docx)
pandas==2.2.3                  # CSV / Excel
openpyxl==3.1.5                # Excel 支援
beautifulsoup4==4.12.3         # HTML 解析
lxml==5.3.0                    # XML / HTML
python-pptx==1.0.2             # PowerPoint
unstructured[all-docs]==0.15.14 # 萬能文件解析（可選，較大）

# --- Embedding ---
sentence-transformers==3.2.1   # 本地 Embedding 備用

# --- 前端 ---
streamlit==1.39.0
streamlit-chat==0.1.1          # 聊天 UI 元件

# --- 驗証 & 評估（防幻覺）---
ragas==0.1.21                  # RAG 評估框架
datasets==3.0.1                # RAGAS 需要

# --- 日誌 & 監控 ---
loguru==0.7.2                  # 結構化日誌
prometheus-client==0.21.0      # 指標監控（可選）

# --- 工具 ---
python-dotenv==1.0.1           # .env 環境變數
pydantic==2.9.2                # 資料驗証
pydantic-settings==2.5.2       # 設定管理
httpx==0.27.2                  # Async HTTP Client
tenacity==9.0.0                # 自動重試
cachetools==5.5.0              # 記憶體快取

# --- 測試 ---
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2                  # FastAPI 測試客戶端
